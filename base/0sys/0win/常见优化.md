#### 思路

```sh
程序安装,卸载
程序设置,检查在线更新,缓存,备份等处

优化思路补充:
进程,服务
启动项,任务计划,注册表
开机主动网络连接情况
使用过程中非用户授意后台联网情况
关闭后,仍然残留后台运行或者网络连接情况
防火墙规则配置
日志查看(报错,大量影响)
```



#### 优化小工具

```sh
#autoruns 清理各种系统残留(启动项,服务,软件,注册表等)

#条目
Everything： 显示所有自动运行的项目。
Logon： 显示用户登录时启动的程序。
Services： 显示系统服务。
Scheduled Tasks： 显示计划任务中运行的项目。
#颜色
粉红色 (Pink)： 没有数字签名或签名无效。这是最需要关注的！这意味着该软件的开发者身份未经过微软或其他权威机构验证。
黄色 (Yellow)： 文件丢失。表示注册表中登记了这个启动项，但在对应的文件夹里找不到,你可以安全地右键删除这些黄色条目。
	--不可删的黄色： 位于 C:\Windows\System32\Drivers 或名称带有 wow64、ntoskrnl 等字样的系统项
绿色 (Green)： 与上一次扫描相比，新增加的启动项
蓝色 (Blue)： 选中的当前行
无底色（白色/黑色）： 正常的、拥有合法数字签名的软件
--------------------------
#tcpview 显示网络连接,端口占用等,包括进程地址,软件名称,厂商等
#颜色
绿色 (Green)： 新建立的连接。表示某个程序刚刚打开了一个端口或发起了一个新请求。
	--如果屏幕经常闪动绿色连接，但你并没有在操作电脑，说明有后台软件正在高频联网
黄色 (Yellow)： 状态改变。例如一个连接从“正在连接”变成了“已建立”，或者端口号发生了变化。
红色 (Red)： 已断开/删除。表示该连接刚刚关闭或进程已终止。它会闪烁红色后从列表中消失。
--------------------------
#process explorer 进程明显查询
#颜色
粉色:	     Windows 服务 代表正在运行系统服务的进程
紫色:		 当前用户进程	由运行 Process Explorer 的同一用户账户启动的进程
深蓝色:	已压缩/加密图像	可能包含加壳后的可执行文件。注意：许多恶意软件会加壳，但也包括某些合法软件
灰色:		 挂起进程	该进程目前处于暂停状态，不占用 CPU 资源
白色:		 普通进程	不符合上述任何特定条件的标准进程

浅绿色:	新创建进程	进程刚刚启动时会显示绿色，通常持续 1 秒左右
红色 :	 已终止进程	进程正在退出或已被关闭
青色:		 UWP/Store 应用	代表从 Windows Store 下载安装的通用平台应用

```

**Local Address 含义**

```sh
#Local Address（本地地址） 常见的几种写法及其含义
0.0.0.0:端口号			#监听所有IPv4地址的指定端口
127.0.0.1:端口号		#仅监听本地回环地址（loopback），只能本机自己访问，局域网/外网无法访问
192.168.0.109:端口号	#只在本机的这个局域网IP上监听 允许局域网/无线网内访问

[::]:端口号			#IPv6全地址监听
[::1]:端口号			#IPv6的本地回环地址（相当于IPv4的127.0.0.1）

# Windows IPv6的本地链路地址
#用于同一局域网内无路由器时的自动通信（类似169.254.x.x）
[fe80::xxxx:xxxx:xxxx:xxxx%16]:端口号	
-----------------------------------------

#Remote Address（远程地址） 常见的写法
0.0.0.0:* 		#表示这是一个监听端口(还没有任何客户端连接进来) 或者UDP端口(UDP本身无连接概念)
*:* 			#同上
[::]:* 			#同上

192.168.0.108:xxx	#有一个客户端(IP是192.168.0.108)正在连接到你本机的某个端口
123.45.67.89:443	#来自公网IP 123.45.67.89 的客户端正在连接你本机的某个端口
本机IP:xxxxx		   #通常是127.0.0.1 或 ::1，表示本机内部程序在互相通信

===============
TIME_WAIT	#等待关闭
SYN_SENT	#请求被拦截
```



#### Windows推送通知服务

```sh
Windows 推送通知系统服务	#服务名:WpnService	 不建议关闭
	#但是该服务运行时会与外部通信,需要关闭
	#防火墙建立出站规则:
	自定义 -> 程序路径：%SystemRoot%\system32\svchost.exe -> 服务：WpnService
	-> 操作：阻止连接 -> 适用于所有网络（公用、私有、域）
	#设置完,它就不能联网,但本地通知可用
-----------------------
```

#### 阻止windows搜索联网

```sh
searchhost.exe	#在线搜索 的主动联网行为 
	1.防火墙出站规则,阻止连接
		C:\Windows\SystemApps\MicrosoftWindows.Client.CBS_cw5n1h2txyewy\SearchHost.exe	#程序位置
	2.注册表关闭网页建议
	按下 Win + R，输入 regedit 回车。
	定位到：HKEY_CURRENT_USER\Software\Policies\Microsoft\Windows
	右键点击 Windows 文件夹 -> 新建 -> 项，命名为 Explorer。
	在 Explorer 项右侧空白处右键 -> 新建 -> DWORD (32 位) 值。
	命名为 DisableSearchBoxSuggestions，并将值改为 1。
```



#### chrome优化

```sh
#优化:
1.打开 Chrome，点击右上角三个点 > 设置:
	#搜索“后台” -> 关闭 Chrome 时继续运行后台应用,取消勾选
	#其他设置,比如广告隐私等,检查下
2.开始->运行:msconfig->启动项,找到google chrome,禁用
3.打开 任务计划->google update相关,禁用
4.打开 系统服务 检查google相关服务,确保没有自动启动
5.使用autoruns工具查看,注册表下google相关启动项,取消勾选
	#发现2个:
	#chrmstp.exe: 这是 Chrome 的安装/配置助手
	#platform_experience_helper.exe:这是 Chrome 的后台辅助进程,Chrome 为了让某些功能（如扩展通知、插件加载）更快启动，会在登录时预加载它
	
#性能->开启省内存模式
#性能->关闭预加载
---------------------------
#禁用通知
	地址栏输入:chrome://settings/content/notifications
	关闭 “网站可以显示通知”
	
#chrome在本机监听的5353端口 :mDNS（Multicast DNS） 的功能,自动发现局域网里的设备
	#关闭监听:
	1.导航到以下路径（如果中间文件夹不存在，就右键新建）
		HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Google\Chrome
	2.右键 Chrome → 新建 → DWORD (32位) 值
	3.命名为 EnableMediaRouter
	4.双击它，把数值数据设为 0（表示禁用）。
	5.完成后，重启即可 (打开 chrome://policy，你应该看到 EnableMediaRouter 显示为 false)
	  
```

#### Navdia优化

```sh
NVIDIA 5个进程,3个服务,9个任务计划?
#清理:
1.在程序与功能里卸载 GeForce Experience 
2.服务里面 NVIDIA 相关服务停止并关闭自动启动
3.任务计划里所有 NVIDIA 条目都禁用

#游戏:
玩游戏前打开nvidia控制面板就行了
```

#### typora优化

```sh
typora.exe 	#每次打开一个md文件,都主动联网
	文件->偏好设置->通用->
		取消:发送匿名数据
		打开高级设置:json文件注释 //search google

#清理
进入 文件 -> 偏好设置 -> 常规，找到 “保存与恢复” 选项。
点击 “恢复未保存的草稿” 按钮，手动删除里面不需要的旧文件
```

#### 常见进程

```sh
services.exe
	--是 Windows RPC/DCOM 服务的动态端口分配
	--记得确保路径是这个下面 => C:\Windows\System32\
	
WUDFHost.exe
	--微软官方组件 用户模式驱动主机
	--你插上一个 USB 设备或蓝牙耳机等设备时，系统会启动一个或多个 WUDFHost.exe 实例来处理这个设备的通信
	
lsass.exe 	#系统进程
wininit.exe #系统进程

ctfmon.exe	#ctf加载程序 输入法相关

perfmon.exe	#性能监视器
resmon.exe	#资源监视器

SHA1:0x39aaf99c	
	--是 Intel® PROSet/Wireless WiFi 软件的一个显示 Bug, 真实名字:Intel_PIE_Service.exe
	--这是英特尔（Intel）无线网卡驱动配套的服务
	
backgroundTaskHost.exe
	--极其重要的“通用宿主进程”
	--Windows 为了节省电量和资源，不允许所有软件都在后台保持全速运行。
```



#### 常见服务

```sh
RpcSs	
	--显示名称 Remote Procedure Call (RPC) 
	--rpc远程调用 135端口
RpcEptMapper	
	--显示名:RPC Endpoint Mapper	
	--rpc远程调用相关
-----------------------
jhi_service	
	--显示名称:Intel(R) Dynamic Application Loader Host Interface Service
	--对应进程: jhi_service.exe 
	--是 Intel 官方的合法进程
	--主要作用:让 Windows 上的应用程序通过 jhi.dll 与 Intel 芯片内置的 Management Engine（ME）固件通信
	
dptftcs	
	--显示名称:Intel(R) Dynamic Tuning Technology Telemetry Service
	--对应进程:ipfsvc.exe
	--主要用于:管理 Intel 处理器的平台级服务:功耗与散热管理,性能优化,传感器支持

ipfsvc	
	--显示名:Intel(R) Innovation Platform Framework Service
	--对应进程:ipfsvc.exe,ipf_uf.exe ipf_helper.exe
	--inter innovation 相关
	
iphlpsvc
	--显示名:IP Helper
	--ipv6相关
Dnscache
	--显示名:DNS Client
	--dns缓存相关,监听5353端口
	
AppXSvc
	--显示名:AppX Deployment Service (AppXSVC)
	--对应进程:wsappx
	--windows系统内置组件的维护(如照片,计算器等)

CDPUser_53c2a	
	--Windows 系统中“连接设备平台用户服务”（Connected Devices Platform User Service）的一个特定实例
	--负责 Windows 与其他设备（如蓝牙耳机、手机、Xbox、智能投影仪等）之间的连接与通信
	--禁用后，你的 蓝牙音频设备 可能无法正常连接
	
TextInputManagementService
	--对应进程:TextInputHost	#显示为:Windows 输入体验
	--负责一切与用户交互输入相关的视觉界面

wlidsvc
	--显示名称:Microsoft Account Sign-in Assistant
	--微软帐户登录助手,处理所有与 Microsoft 帐户（MSA） 相关的身份验证逻辑
	
MsMpEng		#Microsoft Defender的核心后台进程
MDCoreSvc 	#同上
	
IKEEXT	#IPSec vpn相关 500端口
```

#### 可关闭的服务

```sh
server		#局域网共享相关,文件打印等
workstation	#局域网访问相关(网络邻居等)
Remote Procedure Call (RPC) Locator	#过时的服务
Remote Registry		#远程注册表服务,(被别人远程连接访问注册表的),关闭

Print Spooler 	#后台打印程序,没有打印机,则禁用
Secondary Logon #二次登录,用不到,禁用
TCP/IP NetBIOS Helper	#局域网共享相关,禁用
Connected User Experiences and Telemetry 	#连接用户体验和遥测,收集系统错误、使用习惯并上传给微软,禁用

#程序兼容性助手 监控软件运行，当你运行旧软件时弹出“该程序可能安装不正确”的提示
Program Compatibility Assistant Service 	#禁用

Windows Search		#为文件、邮件提供索引	如果你平时很少用自带的搜索,禁用

Windows Error Reporting Service #程序崩溃时向微软发送报告 禁用
#分布式链接跟踪客户端 跟踪局域网内 NTFS 文件之间的链接（如快捷方式指向的移动）
Distributed Link Tracking Client 	#禁用

Microsoft Store Install Service		#微软商店同步/更新服务 禁用

Geolocation service  #位置跟踪 禁用

SSDPSRV
	--显示名:SSDP Discovery ,Simple Service Discovery Protocol 服务
	--主要用于 UPnP（通用即插即用） 设备发现,局域网广播/监听,1900端口
	--在服务里,禁用即可
==================================
DoSvc 	#全称是 Delivery Optimization（传递优化服务）
	在服务里将其启动类型改为 “禁用” ,如提示拒绝访问,则修改注册表:
	修改注册表 HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\DoSvc 中的 Start 为 4）
	
#关闭7680(windows更新传递优化)
打开 设置 -> Windows 更新 -> 高级选项 找到 “传递优化”。
关闭 “允许从其他设备下载” 的开关
```



#### 其他

```sh
dolby access		#杜比音效
OneDrive			#微软云存储
xbox live			#微软游戏框架
outlook				#邮件,日程管理
```

