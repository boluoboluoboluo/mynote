

#### 文件批量重命名

根据输入的名字,对脚本当前目录下的所有文件,按顺序重命名,后缀保持不变,

比如: newname1.jpg , newname2.jpg , newname3.jpg

新建文件  `test.py`

```py
import os
import shutil
import sys

# 根据输入的名字,对脚本当前目录下的所有文件,按顺序重命名,后缀保持不变,
# 比如:newname1.jpg , newname2.jpg , newname3.jpg
name = input("请请输入名字:")
scriptname = "test.py"

dir = r"./"
i = 1
for file in os.listdir(dir):
	if os.path.isdir(os.path.join(dir,file)):
		continue
	if file == scriptname:continue
	print(file)
	name,extension = os.path.splitext(f)	# 后缀包含 . 号
	os.rename(os.path.join(dir,file),os.path.join(dir,name+str(i)+extension))
	i = i+1

print("done.")
```

