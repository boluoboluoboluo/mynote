=================================

requests:
请求:
	请求头(User-Agent,cookie,content-type,refer...)
	超时时间,(流式下载),(响应是否压缩)
	使用会话(关闭连接)
	
	
响应:
	返回码
	响应类型(文本,二进制)
	响应处理(编码)
	
问题:
	请求超时,报错处理
	异常处理,日志打印
	资源释放,关闭连接
	多线程(异步)
		(分块读取?(请求过程部分报错,导致当前请求的数据作废?))
		防止服务器限制,需要单独session吗,需要不同代理ip吗
	重试机制,断点续传
	获取实时连接状态(握手?数据等待中?连接中?完成?)
	下载状态:
		下载进度,时长,(已下载,还剩余?)
		下载耗时?数据大小
		下载速率
	延迟测试?
	关于ssl(verify)
	关于重定向
	代理请求(代理不稳定排查?代理协议?)
		#不能ping,因为代理软件只接收tcp/udp协议
		#curl 测试
		curl -x http://127.0.0.1:7890 -v https://google.com
		#for循环,20次,查看状态码和响应时间
		for /l %i in (1,1,20) do @(curl -o NUL -s -w "%{http_code} - %{time_total}s\n" -x http://127.0.0.1:7890 https://google.com)
		#如果有 000 或 502，说明代理链路存在瞬时波动

	反爬应对
		请求头识别(User-Agent)
		频繁请求限制(ip限制)
		请求规律性检查
		并发限制,长连接限制
		token校验
		验证码

	# :
	# 1.下载的文件过大,大小限制 和 下载写入临时文件
	# 2.分片过多,request请求异步设计,以及保持会话设计,请求重试设计
	# 3.超时问题,前端请求超时,网络请求超时等
	# 4.异常,资源关闭,报错打印优化等
	# 5.ip被盯上,拒绝握手

=================================
note:
	1.视频数据被 br 算法压缩:
		数据无法下载,获取到的分片m3u8内容显示乱码:
		但是浏览器network面板,看到内容正常,也无加密
		发现此请求的 content-encoding:br	#说明服务器采用了压缩算法Brotli
		解决方案:
			session.headers.update({
				# 告诉服务器：请直接给我原始数据，别压缩.
				# 'Accept-Encoding': 'identity', 
			})
		如果服务器不理会这个请求头,仍旧压缩,则需要把获取到的数据解压,解决方法:
			pip install brotli		#安装后,则会对数据自动解压,不需要额外编码